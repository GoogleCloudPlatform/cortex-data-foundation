[holiday]
country_list = US,UK,CA
# Number of past years to load, including the current year.
# Example: If current year is 2024 and this number is 5, then
# holidays for years 2020 to 2024 (inclusive) will be loaded.
years_to_load = 5
dataset_cdc_processed = {{ k9_datasets_processing }}
project_id = {{ project_id_src }}
location = {{ location }}
write_mode = replace
