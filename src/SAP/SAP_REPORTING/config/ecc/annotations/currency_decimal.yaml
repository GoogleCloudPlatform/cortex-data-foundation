name: "{{ project_id_tgt }}.{{ dataset_reporting_tgt }}.currency_decimal"
description: "This table gives information about the decimal factors needed for certain specific currencies, for example JPY, which is required for displaying the amounts correctly. The granularity of this table is at currency key."
catalog_tags:
  - display_name: "line_of_business"
    fields:
      - display_name: "group"
        value: "Utility"
fields:
  - name: "CURRKEY"
    description: "Currency Key, PK"
  - name: "CURRFIX"
    description: "Number of Decimal Places"
