name: "{{ project_id_src }}.{{ dataset_cdc_processed }}.tvfs"
description: "Billing: Reasons for Blocking"
fields:
  - name: "faksp"
    description: "Block"
  - name: "mandt"
    description: "Client (Mandant)"
