name: "{{ project_id_src }}.{{ dataset_raw_landing }}.tvfs"
description: "Billing: Reasons for Blocking"
fields:
  - name: "faksp"
    description: "Billing Block"
  - name: "mandt"
    description: "Client (Mandant)"
